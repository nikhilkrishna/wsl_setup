{
  "permissions": {
    "allow": [
      "Bash(nix search nixpkgs kubelogin)",
      "Bash(nix search nixpkgs kafka)",
      "Bash(nix search nixpkgs kafkactl)",
      "Bash(nix-shell -p kafkactl --run \"kafkactl --help\")"
    ]
  }
}
